# @repo/api

NestJS 11 API with Neon Postgres and JWT sessions.

- Local: `npm run api` from the repo root (port `API_PORT`, default 3001)
- Production: compiled to `dist/` and served by `/api/index.js` on Vercel
